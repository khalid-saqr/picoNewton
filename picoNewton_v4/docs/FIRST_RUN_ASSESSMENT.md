# Assessment of archived run `20260719_043211_UTC_c9da`

This document records the interpretation of the solved historical Colab run without modifying the notebook or its archived Drive output.

Using the corrected endpoint rules:

- all 20 hypothesis/target pairs fail the predeclared current criterion;
- 17 of 20 pairs exceed the exploratory calcium threshold;
- the calcium result cannot rescue a failed current decision;
- all 24 primary pathway-versus-zero comparisons decrease current RMS magnitude;
- signed force and exposure are spatially different but aggregate-indistinguishable in all six arteries at the frozen symmetric parameterization;
- pressure clipping is present in primary aortic-root WSS and vector pathways;
- the correct outcome is `negative_under_current_parameterization`;
- biological claims remain disabled.

This is a valid completed screening result. It must not be converted into a positive result by post hoc parameter tuning. Future runs should use `piconewton-v4-study`, preserve current as the primary endpoint, and satisfy the gates in `SCIENTIFIC_COMPLETION_PATH.md` before any positive mechanosensory claim is considered.
